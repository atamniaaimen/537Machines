rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development: allow all reads/writes (open for testing)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
